## 1. 快速建议入口精简

- [x] 1.1 移除快速建议区“当前阶段”下拉控件与对应前端状态
- [x] 1.2 调整快速建议生成参数与接口调用，清理不再使用的 `targetStage` 传递链路
- [x] 1.3 在建议档位旁增加明确策略文案（标准档“优先当前阶段，不足补邻近阶段”）

## 2. 依赖编辑交互重构（行内按钮 -> 弹窗）

- [x] 2.1 将草稿表“紧前任务”列改为“编辑依赖”行内按钮
- [x] 2.2 新增依赖编辑弹窗，支持任务搜索、多选、已选回显与保存
- [x] 2.3 保存依赖后回写草稿行并实时刷新紧后关系展示
- [x] 2.4 保持草稿转入分解列表前的依赖校验（自依赖/环/时间冲突）并与弹窗修复链路打通

## 3. 阻断提示改造为强提示弹窗

- [x] 3.1 识别 WBS 批量提交中的后端 409 阻断错误（未审批且影响基线）
- [x] 3.2 将阻断错误由顶部提示改为弹窗提示，文案包含“问题是什么、怎么改、怎么操作”
- [x] 3.3 在弹窗中提供“去处理变更单”入口并保留当前草稿上下文
- [x] 3.4 将信息/警告/阻断错误提示分层固化，避免语义混用

## 4. 联调与回归

- [ ] 4.1 完成端到端联调：生成建议 -> 依赖弹窗编辑 -> 批量加入 -> 提交
- [ ] 4.2 验证阻断弹窗链路：触发 409 -> 跳转处理 -> 返回后继续提交
- [ ] 4.3 回归布局与可用性：草稿区表格、弹窗交互、分页/滚动场景下可读性
  - 阻塞说明：`npm run test:api-regression` 依赖数据库 `127.0.0.1:3306`，当前不可达；待数据库可用后补齐联调与回归。
