## 1. 数据模型与迁移

- [x] 1.1 在 `backend/prisma/schema.prisma` 为 `WbsTask` 增加 `wbsCode`、`parentTaskId`、`predecessorTaskIds`、`milestoneId`、`sortOrder` 等结构化字段并补充索引
- [x] 1.2 生成并校验 Prisma migration，确保旧数据可兼容（新增字段可空）
- [x] 1.3 为里程碑与任务关联补充必要的约束与查询索引

## 2. 后端 API 与校验能力

- [x] 2.1 扩展 `createWbsSchema` 与相关校验，支持层级、依赖、里程碑关联字段
- [x] 2.2 在 `store` 中实现 WBS 依赖合法性校验（至少含依赖环检测与日期冲突检测）
- [x] 2.3 新增 `POST /wbs/batch` 批量写入接口，保证事务一致性和逐行错误返回
- [x] 2.4 新增计划一致性校验接口（如 `POST /wbs/validate-plan`），返回行级/字段级冲突
- [x] 2.5 扩展里程碑查询，返回关联任务摘要并支持工作台筛选参数

## 3. 计划编排工作台前端

- [x] 3.1 新增计划编排工作台页面与路由入口，建立统一项目/阶段/时间筛选上下文
- [x] 3.2 在工作台集成 WBS 批量录入区，支持连续新增、复制继承、草稿保留
- [x] 3.3 在工作台集成里程碑列表/时间线视图，支持与筛选器联动
- [x] 3.4 实现列表与图表双向定位（点击甘特节点定位到对应行，反向高亮）

## 4. 甘特图集成与交互增强

- [x] 4.1 扩展 `GanttChart` 数据映射，渲染任务依赖与里程碑统一图层
- [x] 4.2 增加阶段/时间窗联动过滤，确保与列表筛选结果一致
- [x] 4.3 增加关键任务、延期、里程碑等图例和样式规范
- [x] 4.4 为计划冲突项提供图表侧可视化标记与提示

## 5. 联调与验收

- [x] 5.1 编写并执行后端回归测试（单条创建、批量创建、校验失败回滚）
- [ ] 5.2 编写并执行前端关键流程回归（快速录入、过滤联动、双向定位）
- [x] 5.3 补充变更说明与迁移说明，明确旧入口兼容与回滚策略
- [x] 5.4 依据 specs 场景逐条验收并修复差异

> 备注：数据库迁移已通过 `prisma db execute` 应用到现有库后，`npm run test:api-regression` 已通过。5.2 仍需浏览器交互回归。
