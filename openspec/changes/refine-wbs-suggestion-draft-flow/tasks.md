## 1. 草稿确认流改造

- [x] 1.1 在快速分解弹窗引入 `draftRows` 状态，区分建议草稿与已确认分解列表
- [x] 1.2 调整“生成建议”行为为写入草稿区，不直接写入分解列表
- [x] 1.3 实现草稿区批量勾选与“批量加入分解列表”动作
- [x] 1.4 实现草稿加入后状态迁移与失败行保留策略

## 2. 阶段感知规则生成

- [x] 2.1 扩展规则建议接口支持 `mode`（轻量/标准/完整）与 `targetStage`
- [x] 2.2 实现标准档位按当前阶段生成 2-4 条建议的模板策略
- [x] 2.3 实现档位切换后的重生成与提示文案同步
- [x] 2.4 补充规则未命中时的默认回退与提示

## 3. 关联性优先交互

- [x] 3.1 在草稿区支持紧前任务名称多选并写入依赖字段
- [x] 3.2 在草稿区实时反推并展示紧后任务
- [x] 3.3 在“加入分解列表”前执行依赖校验（自依赖/环/时间冲突）
- [x] 3.4 对冲突提供行级定位与可操作提示

## 4. 手动表单按需展开

- [x] 4.1 将完整手动填写区默认折叠，新增展开/收起开关
- [x] 4.2 保证手动新增任务可加入同一分解列表并复用同样校验
- [x] 4.3 优化默认页面布局，突出“快速生成 + 草稿确认”主路径

## 5. 提示分级与联调验收

- [x] 5.1 将生成成功提示改为信息态，移除与校验告警混用
- [x] 5.2 建立信息/警告/错误三级提示映射及统一文案
- [x] 5.3 完成端到端联调：生成草稿 -> 编辑依赖 -> 批量加入 -> 提交
- [ ] 5.4 执行关键回归并记录：阶段感知生成、草稿迁移、依赖冲突定位、手动展开补录
  - 阻塞说明：当前环境 `npm run test:api-regression` 依赖数据库 `127.0.0.1:3306`，连接失败，待数据库可用后补跑并记录。
