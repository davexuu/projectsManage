## 1. 需求梳理与基线确认

- [x] 1.1 盘点当前 WBS 新增/编辑表单字段清单，标注现状（必填/可选/接口必传/页面展示位置）
- [ ] 1.2 与业务确认“可降级为可选或自动推导”的字段清单，并记录未决项（如人物分解项枚举、说明文案）
- [x] 1.3 确认当前项目是否已有通用表格组件封装及可复用主题扩展入口（优先复用现有组件）

## 2. 字段配置与填写要求能力（wbs-form-guidance）

- [x] 2.1 定义 WBS 表单字段元数据结构（label、required、priority、placeholder、helpText、example、validator、visibleWhen、defaultValueResolver）
- [x] 2.2 将现有 WBS 新增/编辑字段映射到字段元数据配置，并按必填/建议/可选/自动推导分级
- [x] 2.3 实现字段填写要求渲染（关键字段说明、示例、格式提示）并支持新增/编辑场景复用
- [x] 2.4 实现“填写要求提示”和“校验错误提示”分离展示逻辑
- [x] 2.5 实现可选字段“更多字段”折叠区与条件显示逻辑（visibleWhen）
- [ ] 2.6 验证降级为可选字段后的提交兼容性（空值/默认值/推导值映射不破坏现有接口）

## 3. WBS 录入流程重构（wbs-entry-flow）

- [x] 3.1 重构 WBS 新增/编辑页面布局为“基础信息区 + 分解项明细区”分段结构
- [x] 3.2 实现从项目入口进入时的项目上下文预填逻辑（项目、版本/迭代等）
- [x] 3.3 实现通用入口进入时的关键上下文字段前置校验与未完成前禁止提交规则
- [x] 3.4 在页面中统一基础信息与分解项区的提交数据模型，保证未提交编辑状态可保留

## 4. 分解项表格化录入与校验（wbs-entry-flow）

- [x] 4.1 实现分解项表格化多行录入（新增行、删除行）并保留行编辑状态
- [x] 4.2 实现分解项复制行功能，并限制仅复制允许继承的字段
- [x] 4.3 实现新增行默认值继承逻辑，确保不覆盖用户在当前行手动输入的值
- [x] 4.4 实现分解项行级/字段级校验定位与错误高亮
- [x] 4.5 实现提交前错误摘要并支持定位到具体行列，且校验失败不丢失已输入数据

## 5. 统一表格样式标准落地（table-ui-standard）

- [x] 5.1 定义表格样式 token/主题变量（表头、边框、行高、间距、hover、斑马纹、空态、加载态）
- [x] 5.2 在通用表格组件封装中添加统一样式接入方式（如主题/variant/样式类）；若无封装则为 WBS 页面抽取可复用样式层
- [x] 5.3 将 WBS 列表页接入统一表格样式标准并校验类 ElementUI Table 的视觉一致性
- [x] 5.4 将 WBS 分解项录入表格接入统一表格样式标准并保证编辑态样式可用
- [x] 5.5 实现统一空状态与加载态样式，确保布局稳定且不遮挡主要操作入口

## 6. 联调、验证与发布准备

- [ ] 6.1 联调 WBS 新增/编辑接口，验证字段精简与默认值推导后的请求/响应兼容性
- [x] 6.2 补充关键场景验证清单（项目预填、通用入口前置校验、批量录入、复制行、行级报错、空态/加载态）
- [x] 6.3 评估并接入页面级开关或配置开关，支持回滚到旧录入流程/旧表格样式
- [ ] 6.4 收集试用反馈（录入效率、错误率、样式可读性）并整理后续优化项
