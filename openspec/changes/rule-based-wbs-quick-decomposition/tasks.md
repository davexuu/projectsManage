## 1. 规则引擎与接口准备

- [x] 1.1 设计并实现“我要做什么”意图分类规则（新增/修复/优化/合规）及关键词匹配策略
- [x] 1.2 实现规则模板到 WBS 建议行的映射逻辑，输出与现有 WBS 批量字段兼容的数据结构
- [x] 1.3 在后端新增规则拆解预生成接口（或等效服务入口），返回意图与建议分解行
- [x] 1.4 为规则拆解接口补充输入校验与异常兜底（未命中关键词的默认策略）

## 2. WBS 自动编码能力

- [x] 2.1 定义 WBS 自动编码规则（层级/排序/并发冲突处理）并在设计文档落地为实现约束
- [x] 2.2 在 WBS 单条与批量创建链路中接入自动编码逻辑（仅对缺失编码输入生效）
- [x] 2.3 确保自动编码结果通过现有 `createWbsSchema` 格式校验并保持查询排序稳定
- [x] 2.4 增加自动编码相关回归测试（包含批量写入与兼容旧数据场景）

## 3. 快速分解前端交互改造

- [x] 3.1 在快速分解弹窗增加“我要做什么”输入与“生成建议”入口
- [x] 3.2 将规则引擎返回的建议行接入现有分解项列表，支持编辑/删除后再提交
- [x] 3.3 调整 WBS 编码字段为只读/自动展示态，并提供编码规则提示
- [x] 3.4 将关键字段说明改为 icon 悬浮（Tooltip/Popover），与校验错误提示分离

## 4. 前置/后继关联交互

- [x] 4.1 在快速分解中实现前置任务可视化选择器（名称检索、多选、回填 ID）
- [x] 4.2 在列表或详情信息中增加紧前/紧后关系展示，保证双向可见
- [x] 4.3 接入现有 `validate-plan` 冲突结果，增强行级定位与冲突提示完整性
- [x] 4.4 对大任务量场景增加筛选或搜索优化，保证依赖选择可用性

## 5. 联调与验收

- [ ] 5.1 完成端到端联调：单句输入生成建议 -> 编辑 -> 计划校验 -> 批量提交
- [ ] 5.2 执行并记录关键回归：自动编码、依赖关联、图标提示、错误定位
- [x] 5.3 更新变更说明与使用指引，明确规则引擎当前支持范围与限制
